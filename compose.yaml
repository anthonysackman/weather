services:
  weather-api:
    build: .
    environment:
      - DB_PATH=/app/weather_display.db
    env_file:
      - ${ENV_FILE:-/etc/weather/.env}
    volumes:
      - ${DB_FILE:-/etc/weather/weather_display.db}:/app/weather_display.db
    ports:
      - "8000:8000"
    restart: unless-stopped

